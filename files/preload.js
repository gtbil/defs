// Intentionally minimal. The legacy BatchPrimer3 UI is plain server-rendered
// HTML/CGI and needs no renderer<->main bridge. Kept here so contextIsolation
// stays on and the wiring is obvious if you later want to expose anything.
